#include <bits/stdc++.h>

using namespace std;

string solution(string s) {
    string answer = "";
    int middle;
    if(s.size() % 2 == 0) {
        middle = s.size();
    }
    return answer;
    
}

int main() {
    
}
